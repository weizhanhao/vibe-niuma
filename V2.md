# vibe-niuma v2

> **每个项目是一个空间；所有对它有需求的人只提需求；每条需求在隔离环境里并行跑完
> 「澄清 → 拆解 → 实现 → 验证 → 复核 → 预览 → 审核 → 合并 → 上线」；
> 合并期的冲突由 AI 解决。**

v1（`orchestrator/` + `extension/`）保留不动，v2 在 `platform/` + `web/` 全新构建。

## 五分钟看明白

```bash
./run-demo.sh            # 工位隔离 + 冲突三档，不需要任何 key
./run-demo.sh serve      # 起后端 + 前端，浏览器里点
```

细节见 [DEMO.md](DEMO.md)。

## 目录

| 目录 | 是什么 |
|---|---|
| `platform/` | v2 后端。分 10 层，接缝有 CI 守 |
| `web/` | React 控制台 |
| `platform-skills/` | 流水线各环节的可插拔 skill（vendored + patch + 自研） |
| `demo-target/` | demo 用的两个真 git 仓（Python + TS，测试能跑） |
| `spikes/review-filter/` | 已实测的复核过滤层 |
| `deploy/v2/` | compose + workspace 镜像 + API 镜像 |
| `docs/superpowers/specs/2026-08-24-*.md` | 设计文档，12 个 ADR + 实测数据 |
| `docs/mockups/v2-web-console-demo.html` | 可点击的交互原型 |

## v1 的三个致命问题与 v2 的解

| v1 | v2 |
|---|---|
| `opencode run` 一次性调用，每轮丢会话；续改靠拼历史 + 让 agent 自己 `git diff` 猜 | `agents/` —— session 是一等公民，refine 复用、拆任务 fork |
| 单工作树 `checkout` 切分支，`create_branch` 会 `reset --hard`；两个并发 Run 互相抹改动 | `workspace/` —— 每 Run 独立 worktree + 容器。**demo 实测 5 并行互不污染** |
| `SystemConfig` 是 `id=1` 单例，多项目只存在扩展本地 | `core/models.py` —— Org/Project/Member 多租户，每表带 `project_id`，路由强制过成员校验 |

## 十二个已定决策

| # | 决策 |
|---|---|
| D1 | 纯 Web，放弃浏览器框选 |
| D2 | git worktree 打底 + Docker 包一层 |
| D3 | 自建状态机 + worker，不上 Temporal |
| D4 | 继续用 MySQL 8，不迁 Postgres |
| D5 | `SystemConfig` 单例拆成 Project 表 |
| D6 | AI 自动拆需求，不强制人工确认 |
| D7 | opencode server + session |
| D8 | 声明式 DAG（YAML in DB） |
| D9 | 接 `alibaba/open-code-review`，包 `CodeReviewAdapter` |
| D10 | CD 边界交 `DeployAdapter`；只实现自建，云效延后 |
| D11 | 预览 / 测试 / 生产三层，预发暂缓 |
| D12 | 每个流水线环节 = 一个可插拔 Agent Skill |

## 三条实测得出的硬约束

来自 2026-08-24 对 `ocr` v1.9.10 + `deepseek-v4-pro` 的真实跑测：

1. **`retry_report` 无失败时整个 key 缺失** —— 缺失=零失败，不是未知
2. **`status: complete` + 退出码 0 也可能有失败请求** —— 必须查 `failed_requests`
3. **0 条发现 ≠ 代码干净** —— 同一 diff 三次跑出 2/0/0，UI 只能写「未发现」

## 状态

| | |
|---|---|
| 后端测试 | **157 passed**，sqlite 与 MySQL 8 双跑全绿 |
| 前端测试 | 29 passed，TypeScript 干净 |
| 接缝守卫 | ✓ 5 层完好（守卫自己也有测试） |
| MySQL 专项验证 | 7/7 |
| 迁移 | 版本化迁移器，真 MySQL 上跑通且幂等 |
| CI | sqlite + MySQL 双跑 + 镜像构建 |

经过三方独立审查并逐条修复，见 [AUDIT.md](AUDIT.md)。
**仍未验证的部分在那里如实列出**（容器路径、opencode server、
端到端真实需求闭环）。
