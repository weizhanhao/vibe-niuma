# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
venv/
demo/backend/.env
orchestrator/.env
deploy/.env
deploy/llm-proxy/config.yml

# Node
node_modules/
dist/
*.tsbuildinfo

# 环境
.DS_Store

# Claude Code 会话锁（每次启动重写）
.claude/scheduled_tasks.lock

# Claude Code 临时 worktrees（embedded git repos，不入主仓）
.claude/worktrees/

# uv 锁文件（orchestrator 用 pip + venv，uv.lock 是无关产物）
orchestrator/uv.lock
