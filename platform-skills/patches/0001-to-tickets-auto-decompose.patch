--- a/vendor/to-tickets/SKILL.md
+++ b/vendor/to-tickets/SKILL.md
@@ -39,29 +39,33 @@
 
 **Wide refactors are the exception to vertical slicing.** A **wide refactor** is one mechanical change (rename a column, retype a shared symbol) whose **blast radius** fans across the whole codebase, so a single edit breaks thousands of call sites at once and no vertical slice can land green. Don't force it into a tracer bullet; sequence it as **expand–contract**. First expand: add the new form beside the old so nothing breaks. Then migrate the call sites over in batches sized by blast radius (per package, per directory), each batch its own ticket blocked by the expand, keeping CI green batch to batch because the old form still exists. Finally contract: delete the old form once no caller remains, in a ticket blocked by every migrate batch. When even the batches can't stay green alone, keep the sequence but let them share an integration branch that all block a final integrate-and-verify ticket; green is promised only there.
 
-### 4. Quiz the user
+<!-- vibe-niuma patch --> 识别为 wide refactor 时，**在每个 ticket 上标
+`Sequence: expand | migrate | contract`**。平台据此知道这串 ticket 的 Touches 大面积
+相交是预期的、不是冲突，不要按普通需求的冲突规则卡住它。
 
-Present the proposed breakdown as a numbered list. For each ticket, show:
+### 4. Critic review
 
-- **Title**: short descriptive name
-- **Blocked by**: which other tickets (if any) must complete first
-- **What it delivers**: the end-to-end behaviour this ticket makes work
+<!-- vibe-niuma patch: upstream 这一步是 "Quiz the user"，与 D6（AI 自动拆解，
+     不强制人工确认）冲突。改为交给独立 critic 复核。 -->
 
-Ask the user:
+**不要向人类征求批准。** 拆解是自动的。
 
-- Does the granularity feel right? (too coarse / too fine)
-- Are the blocking edges correct: does each ticket only depend on tickets that genuinely gate it?
-- Should any tickets be merged or split further?
+把拟定的拆分交给一个独立复核者：Call the Skill tool with "decompose-critic".
 
-Iterate until the user approves the breakdown.
+交给它：ticket 列表（Title / Blocked by / What it delivers）、每个 ticket 声明的
+`Repos` 与 `Touches`、以及你提取出的 `Contracts`。
+
+critic 返回 `pass: false` 时，按它的意见修订后重新提交。**连续 2 轮不通过就停止拆分**：
+把整块工作作为单个 ticket 输出，并标 `Degraded: true`，由平台串行执行。
 
 ### 5. Publish the tickets to the configured tracker
 
-Publish the approved tickets. **How** depends on the tracker `/setup-matt-pocock-skills` configured; the tickets are the same either way, only the shape of the blocking edges changes:
+<!-- vibe-niuma patch: 强制 local files 模式。我们的 tracker 是平台自己的
+     Requirement / Task 表，orchestrator 会读这些文件入库，不发 GitHub / Linear。 -->
+
+**始终使用 local files 模式**，忽略任何其他 tracker 配置：
 
 - **Local files** → write one file per ticket under `.scratch/<feature-slug>/issues/<NN>-<slug>.md`, numbered from `01` in dependency order (blockers first). Each file's "Blocked by" lists the numbers/titles it depends on. Use the per-ticket file template below: one ticket per file, never a single combined file.
-- **A real issue tracker (GitHub, Linear, …)** → publish one issue per ticket in dependency order (blockers first) so each ticket's blocking edges can reference real identifiers. Use the platform's native blocking / sub-issue relationship where it has one; otherwise set each ticket's "Blocked by" to the blocking issues. Apply the `ready-for-agent` triage label unless instructed otherwise; the tickets are agent-grabbable by construction.
-
 Work the **frontier**: any ticket whose blockers are all done. For a purely linear chain that means top to bottom.
 
 Do NOT close or modify any parent issue.
@@ -73,6 +77,18 @@
 **What to build:** the end-to-end behaviour this ticket makes work, from the user's perspective, not a layer-by-layer implementation list.
 
 **Blocked by:** the numbers/titles of the tickets that gate this one, or "None (can start immediately)".
+
+**Repos:** the repositories this ticket touches (one line, comma-separated).
+
+**Touches:** the files/modules this ticket is *predicted* to touch, one per line.
+
+<!-- vibe-niuma patch: upstream 说「避免具体文件路径，它们过期很快」——那条针对的是
+     *实现指令*。这里的 Touches 不是实现指令，而是**调度元数据**：平台用它做冲突前置
+     （同需求内 Touches 相交的 ticket 不并行；与 in-flight 需求相交的提前预警排序）。
+     预测不准不影响正确性，只影响调度质量。 -->
+
+**Contracts:** any interface contract this ticket depends on or provides (HTTP route,
+event shape, DB column). 跨仓 ticket **必须**先有契约才能并行。留空写 "None".
 
 **Status:** ready-for-agent
 
